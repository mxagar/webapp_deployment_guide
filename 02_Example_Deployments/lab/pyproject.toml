[project]
name = "vastai-lab"
version = "0.1.0"
description = "Companion environment for the Vast.ai deployment guide notebook (vastai.ipynb)"
requires-python = ">=3.10"
dependencies = [
    "vastai>=0.3.5",
    "openai>=1.50.0",
    "python-dotenv>=1.0.1",
    "httpx>=0.27.0",
    "ipykernel>=6.29.0",
]

[tool.uv]
package = false
